# Install Flask and Selenium
pip install flask selenium

# Run Flask app
python app.py

http://localhost:5000

# Build Docker image from Dockerfile
docker build -t registration-app .

# Run container in detached mode and map port
docker run -d -p 5000:5000 registration-app

# List running containers
docker ps

# Run Selenium test script
python tests/test_registration.py